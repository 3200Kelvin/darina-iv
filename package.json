{
  "name": "iwc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "build:staging": "vite build --mode staging",
    "build:prod": "vite build --mode production",
    "deploy": "npm run build:staging && git add . && git commit -a -m\"deploy\" && git push",
    "deploy:prod": "npm run build:prod && rm -rf ../backend/public/* && cp -r dist/* ../backend/public/ && npm --prefix ../backend run deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^3.6.0",
    "mini-css-extract-plugin": "^2.7.5",
    "path": "^0.12.7",
    "sass-embedded": "^1.97.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@barba/core": "^2.10.3",
    "lenis": "^1.3.11",
    "motion": "12.29.0"
  }
}
